package com.formation.test;

import static org.junit.Assert.*;

import java.sql.SQLException;
import java.util.concurrent.TimeoutException;

import org.junit.Test;

import com.formation.wiki.dao.UserDAO;

public class UserDAOtest {

	@Test
	public void test() {
		fail("Not yet implemented");
	}
	
	@Test
	public static void isUserExist() throws TimeoutException, SQLException {
//		assertTrue(UserDAO.isUserExist("Mila", "Mila"));
//		assertTrue(UserDAO.isUserExist("**", "-*"));
//		assertFalse(UserDAO.isUserExist("NAME", "Mila"));
//		
//		assertFalse(UserDAO.isUserExist(null, null));
//		assertFalse(UserDAO.isUserExist("anything", null));
//		assertFalse(UserDAO.isUserExist(null, "anything"));
//		assertFalse(UserDAO.isUserExist("0", "-*-"));
		
		
	}

}
